// TODO: What will the following line of code log?
console.log(window);
//Will log the window OBJECT

// TODO: What will the following line of code log?
console.log(window.document);
//will log the document in the current window.

// TODO: What will the following line of code log?
console.log(document.documentElement);
//will log a particular element in the document being referenced in the current window...log's the document's root element.

// TODO: What will the following line of code log?
console.log(document.head);
//will log a specific, named element in the document, in this case, the <head></head> element.

console.log(window.location);

console.log(document.h1);

